import httpClient from '../http-common'

const withdraw = (amount : number) => {
    console.log(amount)
}

const deposit = (amount : number) => {
    console.log(amount)
}

const checkBalance = (id : string) => {

}

export default { withdraw, deposit, checkBalance }